{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "p=$CLAUDE_PROJECT_DIR/../calcera-ops/hooks/guard.py; [ -f $p ] || p=../calcera-ops/hooks/guard.py; [ -f $p ] || p=/Users/aashiq/Projects/calcera-ops/hooks/guard.py; [ -f $p ] && python3 $p || true"
          }
        ]
      },
      {
        "matcher": "Read",
        "hooks": [
          {
            "type": "command",
            "command": "p=$CLAUDE_PROJECT_DIR/../calcera-ops/hooks/guard.py; [ -f $p ] || p=../calcera-ops/hooks/guard.py; [ -f $p ] || p=/Users/aashiq/Projects/calcera-ops/hooks/guard.py; [ -f $p ] && python3 $p || true"
          }
        ]
      }
    ]
  }
}
